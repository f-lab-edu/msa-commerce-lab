apis:
  monolith@v1:
    root: docs/api/monolith-openapi.yaml

extends:
  - recommended

rules:
  operation-description: warn
  no-unresolved-refs: error
  no-server-example.com: off
  security-defined: off
